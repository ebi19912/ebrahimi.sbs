# راهنمای آپدیت سایت روی سرور (Git Pull)

هر زمان که تغییری روی سایت انجام شد و روی گیتهاب قرار گرفت، برای آپدیت کردن سرور اصلی به صورت امن (بدون از دست رفتن اطلاعات دیتابیس) همیشه مراحل زیر را به ترتیب اجرا کنید:

---

### مرحله اول: ورود به پوشه پروژه
ابتدا باید وارد پوشه‌ای شوید که کدهای سایت در آن قرار دارد:
```bash
cd /var/www/ebrahimi_website
```

### مرحله دوم: بکاپ گرفتن از دیتابیس فعلی (بسیار مهم ⚠️)
با این دستور از دیتابیسی که روی سرور در حال کار است یک کپی در پوشه قبلی می‌گیریم تا تحت هیچ شرایطی اطلاعات از بین نرود.
```bash
cp instance/database.db ../database_backup.db
```

### مرحله سوم: متوقف کردن سایت (کانتینرها)
قبل از تغییر فایل‌ها بهتر است سرویس‌ها را متوقف کنیم:
```bash
docker compose down
```

### مرحله چهارم: دریافت کدهای جدید از گیتهاب
با دستورات زیر، گیت را مجبور می‌کنیم که دقیقاً با کدهای جدید همگام‌سازی شود:
```bash
git fetch origin main
git reset --hard origin/main
git pull origin main
```

### مرحله پنجم: برگرداندن دیتابیس سرور از بکاپ
حالا دیتابیسی که در مرحله ۲ بکاپ گرفته بودیم را سر جایش برمی‌گردانیم تا داده‌های سایت شما دست‌نخورده باقی بماند:
```bash
cp ../database_backup.db instance/database.db
```

### مرحله ششم: استارت مجدد سایت
وب‌سایت را مجدداً بیلد و اجرا می‌کنیم:
```bash
docker compose up -d --build
```

---

### 💡 نکته مهم برای آپدیت‌های دیتابیس:
گاهی اوقات ساختار دیتابیس تغییر می‌کند (مثلا فیلد جدیدی به پنل ادمین اضافه می‌شود). 
در این مواقع خاص، علاوه بر مراحل بالا، توسعه‌دهنده یک کد کوتاه پایتون به شما می‌دهد. آن کد را باید **دقیقاً بعد از مرحله ۵ و قبل از مرحله ۶** اجرا کنید تا ستون‌های جدید روی دیتابیس قدیمیِ شما هم ساخته شوند. در آپدیت‌های معمولی که فقط ظاهر سایت تغییر می‌کند نیازی به این کار نیست.
